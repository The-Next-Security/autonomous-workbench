# TOOLS.md — Node/TS Specialist

## Skills
- `coding-agent` para trabajo largo de optimización.
- `governance-wrapper`.
- (sin skill propia en `~/.openclaw/skills/` por ahora; pendiente para
  iteraciones futuras).

## Herramientas de profiling
- `node --inspect` y Chrome DevTools.
- `clinic doctor`, `clinic flame`, `clinic bubbleprof`.
- `0x`.
- Heap snapshots y comparación.

## Herramientas de build
- `esbuild`, `tsup`, `swc`.
- `tsc --noEmit` para validación pura de tipos.

## Herramientas de benchmark
- `vitest bench`.
- `mitata` para micro-benchmarks.
- Load testing: `autocannon` o `k6`.

## Triggers
- "memory leak", "CPU", "performance", "event loop", "stream", "worker",
  "tipos avanzados", "generics", "conditional types", "bundle size",
  "perf regresión".
